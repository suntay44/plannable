# /plannable create

Create a scenario-driven Plannable plan.

Equivalent terminal command:

```bash
plannable create "CRM"
plannable create "inventory management app"
```

Do not run `plannable init` first. `create` fully sets up the Plannable structure.
