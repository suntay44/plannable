# /plannable evidence

Record evidence for a real Plannable part.

Equivalent terminal command:

```bash
plannable evidence PART-001 "Implemented and verified the active part" --artifact "npm test"
```

Use before checking off a part.
