# /plannable complete

Check off a Plannable part after evidence exists.

Equivalent terminal command:

```bash
plannable complete PART-001
```

If evidence is not already recorded, use:

```bash
plannable complete PART-001 --summary "Implemented and verified the active part" --artifact "npm test"
```
